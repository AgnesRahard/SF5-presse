{% extends 'base.html.twig' %}

{% block title %}Edit Tag{% endblock %}

{% block body %}
    {# <h1>Edit Tag</h1>

    {{ include('tag/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tag_index') }}">back to list</a>

    {{ include('tag/_delete_form.html.twig') }} #}

    <div class="text-center">
		<h2 class='text-center'>Modifier d'un mot-clé</h2>
		<div class="d-flex justify-content-center">
			<a class='btn btn-primary mx-2' href="{{ path('tag_index') }}">
				<i class="fas fa-undo-alt" alt='Retour à la liste'></i>
				Retour à la liste
			</a>
		</div>
	</div>
	{{ include('tag/_form.html.twig') }}

	{{ include('tag/_delete_form.html.twig') }}
{% endblock %}
