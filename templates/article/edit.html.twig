{% extends 'base.html.twig' %}

{% block title %}Modifier article
{% endblock %}

{% block body %}
	{# <h1>Modifier un article</h1> #}

	{# {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('article_index') }}">back to list</a>

	{{ include('article/_delete_form.html.twig') }} #}

	<div class="text-center">
		<h2 class='text-center'>Modifier d'un article</h2>
		<div class="d-flex justify-content-center">
			<a class='btn btn-primary mx-2' href="{{ path('article_index') }}">
				<i class="fas fa-undo-alt" alt='Retour à la liste'></i>
				Retour à la liste
			</a>
		</div>
	</div>
	{{ include('article/_form.html.twig') }}

	{{ include('article/_delete_form.html.twig') }}
{% endblock %}
