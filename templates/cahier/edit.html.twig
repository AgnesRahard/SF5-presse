{% extends 'base.html.twig' %}

{% block title %}Modifier cahier{% endblock %}

{% block body %}
    {# <h1>Edit Cahier</h1>

    {{ include('cahier/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('cahier_index') }}">back to list</a> #}

    <div class="text-center">
		<h2 class='text-center'>Modifier d'un cahier</h2>
		<div class="d-flex justify-content-center">
			<a class='btn btn-primary mx-2' href="{{ path('cahier_index') }}">
				<i class="fas fa-undo-alt" alt='Retour à la liste'></i>
				Retour à la liste
			</a>
		</div>
	</div>
	{{ include('cahier/_form.html.twig') }}

    {{ include('cahier/_delete_form.html.twig') }}
{% endblock %}
