{% extends 'base.html.twig' %}

{% block title %}Cahier{% endblock %}

{% block body %}
    <h1>Cahier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cahier.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cahier.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cahier.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cahier_index') }}">back to list</a>

    <a href="{{ path('cahier_edit', {'id': cahier.id}) }}">edit</a>

    {{ include('cahier/_delete_form.html.twig') }}
{% endblock %}
